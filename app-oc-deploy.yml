apiVersion: v1
kind: DeploymentConfig
metadata:
  name: app-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: app-deployment
    spec:
      containers:
      - image: #{IMAGETAG}#
        name: dotnetcodeopenshift
        ports: 
        - containerPort: 80